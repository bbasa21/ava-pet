name: Build AVA PET APK (Legacy)

on:
  workflow_dispatch:

# The Docker workflow is the canonical APK builder.
# This legacy workflow is kept manual-only so pushes to main
# do not start a second, incompatible Buildozer environment.

permissions:
  contents: read

jobs:
  disabled:
    name: Legacy builder disabled
    runs-on: ubuntu-24.04
    steps:
      - name: Explain
        run: echo "Use .github/workflows/build-apk-docker.yml for APK builds."
